      --- SQC DINAMICKI SQL ---

- KADA NEKI DEO SQL NAREDBE NIJE POZNAT U PISANJU PROGRAMA,
  VEC U NJEGOVOM IZVRSAVANJU 

- ANALIZIA I OPTIMIZACIJA SE NEKADA VRSE PRI IZVRSAVANJU 
  PROGRAMA KADA JE POZNAT PLAN IZVRSAVANJA PROGRAMA

- SQL NAREDBA FORMIRA:
  - NAREDBA KOJA NIJE SELECT:
    - POTPUNA 
    - PARAMETRIZOVANA 

  - SELECT 
    - FIKSNA LISTA KOLONA 
    - PROIZVOLJNA LISTA KOLONA 

  - PROIZVOLJNA NAREDBA 

  1.  PRIPREMANJE NAREDBE - IZVRSAVA SE PRIPREMA TEKSTUALNE SQL NAREDBE KOJA MOZE DA SE IZVRSAVA 
      PREPARE <NAZIV_NAREDBE>
      [[OUTPUT] INTO <NAZIV_DESKRIPTORA> | INPUT INTO <NAZIV_DESKRIPTORA>]
      FROM <HOST_PROMENLJIVA_NAREDBE>;

        EXEC SQL 
          PREPARE stmt 
          FROM :naredba;

      - DESCRIBE 
        DESCRIBE stmt INTO :sqlda;

      - EXECUTE (AKO NIJE SELECT)
        EXEC SQL EXECUTE stmt USING <LISTA_HOST_PROMENLJIVIH>;
        EXEC SQL EXECUTE stmt USING DESCRIPTOR <IME_DESKRIPTORA>;


      - DECLARE CURSOR (SELECT) 


  2. SELECT?  ALOKACIJA PROSTORA 

  3. IZVRSAVANJE   

  4. OBRADA 

- EXEC SQL EXECUTE IMMEDIATE :naredba;
  - MOZE DA IZVRSI BILO KOJU NAREDBU KOJA NIJE SELECT NAREDBA 
  - PARAMETRIZOVANA : KADA UMESTO HOST PROMENLJIVE IMAMO ? 

- SQLDA = SQL DESCRIPTION AREA (STRUKTURA PODATAKA)
  - JE STRUKTURA PODATAKA KOJA SE KORISTI KADA KORISTIMO DINAMICKI SQL 
  - SQLN    = BROJ ALOCIRANIH SQLVAR STRUKTURA 
  - SQLDABC = BROJ ALOCIRANIH BAJTOVA
  - SQLD    = BROJ PROMENLJIVIH (KOLIKO KOLONA JE NAVEDENO U SQL UPITU)
  
  - 16B + (SQLD * SIZEOF(STRUCT SQLVAR)) = KOLIKO TREABMO DA ALOCIRAMO 
  - SQLDASIZE(SQLD) = KOLIKO BAJTOVA TREBA DA SE ALOCIRA (PAMETNIJE) (ALOKACIJA MEMORIJE)

  - SQLVAR 
    - sqltype
    - sqllen 
    - sqlname 
      - NAKON DESCRIBE 
    - sqldata 
    - sqlind